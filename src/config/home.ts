import { HomeType } from "../../types"

export const home: HomeType = {
    name: "app name",
    description: "",
    url: "https://localhost:3000",
    imgLink: {
        bg: "",
        headerIcon: "",

    }
}
